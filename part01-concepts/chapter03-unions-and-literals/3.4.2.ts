export let geneticist = Math.random() > 0.5 ? 'Barbara McClintock' : undefined;

if (geneticist) {
  geneticist.toUpperCase();
}

geneticist.toUpperCase();

export let biologist = Math.random() > 0.5 && 'Rachel Carson';

if (biologist) {
  biologist;
} else {
  biologist;
}
